// Essentials
import "@/delta/help";
import "@/delta/start";

// Commands
import "@/delta/show";
import "@/delta/setup";
import "@/delta/links";
import "@/delta/register";
import "@/delta/contribute";

// Now, play time
import "@/delta/exclude";
