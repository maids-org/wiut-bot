// Essentials
import "@/delta/help";
import "@/delta/start";

// Commands
import "@/delta/show";
import "@/delta/setup";
import "@/delta/stats";
import "@/delta/links";
import "@/delta/groups";
import "@/delta/register";
import "@/delta/timetable";
import "@/delta/contribute";

// Now, play time
import "@/delta/exclude";
